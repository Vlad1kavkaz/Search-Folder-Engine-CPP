# Используйте базовый образ, содержащий необходимый набор инструментов (gcc, g++, CMake, etc.)
FROM gcc:latest

# Установка дополнительных библиотек, если это необходимо

# Копирование исходного кода в контейнер
COPY . /app

# Переход в рабочую директорию
WORKDIR /app

# Установка зависимостей и сборка программы
RUN mkdir build \
  && cd build \
  && cmake .. \
  && make

# Задание команды для запуска программы при старте контейнера
CMD ["./build/main.cpp"]